name: Publish protected launcher release
on:
  push:
    tags: ['v*']
permissions:
  contents: write
jobs:
  release:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
      - name: Validate release version
        shell: pwsh
        env:
          RELEASE_TAG: ${{ github.ref_name }}
        run: |
          [xml]$project = Get-Content MistikLauncher/MistikLauncher.csproj
          $version = [string]$project.SelectSingleNode("/Project/PropertyGroup/Version").InnerText
          if ($env:RELEASE_TAG -ne "v$version") { throw 'Tag must match project version' }
      - name: Build protected package
        shell: pwsh
        run: ./scripts/Build-Protected.ps1
      - name: Build online and offline installers
        shell: pwsh
        run: ./scripts/Build-Installers.ps1
      - name: Publish release
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ github.ref_name }}
        run: |
          $version = $env:RELEASE_TAG.Substring(1)
          # Locally published signed releases take precedence; never replace existing assets.
          gh release view $env:RELEASE_TAG *> $null
          if ($LASTEXITCODE -eq 0) { exit 0 }
          $arguments = @('release', 'create', $env:RELEASE_TAG, "artifacts/MistikLauncher-$version-win-x64.zip", "artifacts/installers/MistikSetup-Online-$version.exe", "artifacts/installers/MistikSetup-Offline-$version.exe", 'artifacts/installers/SHA256SUMS.txt', '--title', "Mistik Launcher $version", '--notes-file', 'docs/RELEASE-6.0.1.md')
          if ($version.Contains('-')) { $arguments += '--prerelease' }
          gh @arguments
          if ($LASTEXITCODE) { throw 'Release publication failed' }
